fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pydantic>=2.9,<2.11
email-validator==2.2.0
pydantic-settings>=2.5,<3
PyJWT==2.10.1
argon2-cffi==23.1.0
cryptography==44.0.0
redis==5.2.1
httpx==0.28.1
alembic==1.14.0
python-multipart==0.0.20
croniter==5.0.1
# Calendars. An ICS feed is not a list of appointments but a set of rules: a
# weekly meeting is one entry plus a repetition rule, minus the days it was
# cancelled, plus the ones that were moved. Expanding that correctly — and in
# the event's own time zone, so a standing meeting does not drift by the
# daylight-saving offset — is what the second package is for.
icalendar==6.1.0
recurring-ical-events==3.3.3
aiogram>=3.13,<4
# Mail-Client: IMAP (lesen, Ordner, Entwuerfe) und HTML-Umwandlung fuer die Vorschau.
# Synchron und im Threadpool aufgerufen — dieselbe Bibliothek, die sich im imap-mcp bewaehrt hat.
imapclient>=3.0
html2text>=2024.2.26
# HTML einer fremden Mail saeubern, bevor es ueberhaupt in die Naehe eines Browsers kommt.
# Der Rahmen im Frontend ist abgeschottet (sandbox + CSP) — das hier ist die zweite Tuer.
nh3>=0.2.17
# Tests (laufen im selben Image; Prod-Pfade unberührt)
pytest==8.3.4
pytest-asyncio==0.25.0
aiosqlite==0.20.0
